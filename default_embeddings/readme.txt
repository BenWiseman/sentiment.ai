To keep the package small on CRAN, default embeddings for sentiment_match() are placed here. 

use <emedding_model_name>_<pkg version>.rda

pre-calculated embeddings are used to speed up sentiment_match() if download fails, package will create them when they are needed. 

