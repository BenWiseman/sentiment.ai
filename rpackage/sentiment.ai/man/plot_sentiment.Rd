% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_sentiment}
\alias{plot_sentiment}
\title{Interactive sentiment map}
\usage{
plot_sentiment(
  x,
  model = DEFAULT_MODEL,
  embeddings = NULL,
  rows = NULL,
  reducer = c("auto", "umap", "tsne", "pca"),
  n_clusters = "auto",
  labels = c("tfidf", "openai", "none"),
  color_by = c("sentiment", "class", "style", "hate"),
  api_key = NULL,
  openai_model = "gpt-4o-mini",
  max_label_terms = 3L,
  title = "Sentiment map",
  width = 900,
  height = 640,
  point_size = 8,
  seed = 0,
  return_data = FALSE,
  batch_size = 100,
  ...
)
}
\arguments{
\item{x}{Character vector of texts to map. (Required.)}

\item{model}{character; embedding model handle (default \code{"e5-small"}). See
\code{\link{init_sentiment.ai}} for the options. This is NOT a
tensorflow-hub name.}

\item{embeddings}{Optional precomputed \code{(n, dim)} embedding matrix -- skips the
embed step (and lets the plot run fully offline).}

\item{rows}{Optional precomputed \code{\link{sentiment}()} data.frame to reuse for the
colour / flags; if omitted it is computed from the embeddings.}

\item{reducer}{One of \code{"auto"}, \code{"umap"}, \code{"tsne"}, \code{"pca"}.}

\item{n_clusters}{Number of clusters for the labels, or \code{"auto"}
(\eqn{\approx \sqrt{n/2}}, clamped to \code{[2, 12]}).}

\item{labels}{One of \code{"tfidf"} (deterministic), \code{"openai"} (needs a key), or
\code{"none"}.}

\item{color_by}{One of \code{"sentiment"} (default), \code{"class"}, \code{"style"},
\code{"hate"}.}

\item{api_key}{Optional OpenAI key for \code{labels = "openai"}.}

\item{openai_model}{OpenAI model for the labels (default \code{"gpt-4o-mini"}).}

\item{max_label_terms}{Max terms in a c-TF-IDF label (default 3).}

\item{title, width, height, point_size, seed}{Cosmetic / reproducibility controls.}

\item{return_data}{If \code{TRUE}, return \code{list(plot, data)} where \code{data} is
the underlying data.frame (coordinates, scores, clusters, labels).}

\item{batch_size}{Size of batches to use. Larger numbers will be faster than
smaller numbers, but do not exhaust your system memory!}

\item{...}{Additional arguments passed to \code{\link[reticulate:conda_install]{conda_install()}}
or \code{\link[reticulate:virtualenv_install]{virtualenv_install()}}.}
}
\value{
A \pkg{plotly} htmlwidget (or \code{list(plot, data)} if \code{return_data}).
}
\description{
Plot an interactive semantic map of \code{x} coloured by sentiment. Each
point is one text, positioned by a 2-D projection of its embedding, coloured by
sentiment, labelled on hover with its full text + class + any hate/mixed/style flags,
and grouped into auto-labelled clusters. The R twin of the Python package's
\code{plot_sentiment()}.
}
\details{
Honest by design: the geometry is a \emph{projection} (UMAP via \pkg{uwot} if installed,
else t-SNE via \pkg{Rtsne}, else PCA) -- explore with it, do not measure off it; the
axis titles name the method used. Cluster labels are deterministic c-TF-IDF by default
(\code{labels = "tfidf"}); \code{labels = "openai"} spends a fraction of a cent on
gpt-4o-mini for a tidier 2-4 word topic and silently falls back to c-TF-IDF on failure.

\pkg{plotly} is required (Suggested); \pkg{uwot} and \pkg{Rtsne} are optional.
}
\examples{
\dontrun{
  init_sentiment.ai(model = "e5-small")
  p <- plot_sentiment(airline_tweets$text)
  p                                   # interactive in the viewer
  htmlwidgets::saveWidget(p, "map.html")
}
}
