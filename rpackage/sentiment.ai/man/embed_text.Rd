% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{embed_text}
\alias{embed_text}
\title{Create Text Embedding Matrix}
\usage{
embed_text(
  text,
  batch_size = NULL,
  model = NULL,
  api_key = NULL,
  api_base = "https://api.openai.com",
  api_version = "v1",
  api_type = NULL,
  api_engine = "text-embedding-ada-002",
  request_limit = 3000,
  token_limit = 3e+05
)
}
\arguments{
\item{text}{character vector to be embedded. Note that longer comments take longer.}

\item{batch_size}{integer - how many to embed at once. Higher numbers are faster but use more memory.}

\item{model}{character - the embedding model to use (same as \code{sentiment_score()}).}

\item{api_key}{character string specifying the API key for OpenAI, if using an OpenAI model.}

\item{api_base}{optional character string specifying custom base URL for the OpenAI API.}

\item{api_version}{optional character string specifying custom version for the OpenAI API.}

\item{api_type}{optional character specifying type of API instance, e.g. "azure"}

\item{api_engine}{optional to change openai engine, e.g. "gpt-35-turbo"}

\item{request_limit}{integer - maximum number of requests per minute for OpenAI models. Default is 60.}

\item{token_limit}{integer - maximum number of tokens per minute for OpenAI models. Default is 60000.}
}
\value{
numeric matrix of \verb{length(text) x length(embedding)}. Original text is stored in the row names attribute.
}
\description{
Turns a character vector into a \verb{length(text) x length(embedding)} embedding matrix.
For power users. Requires \code{init_sentiment.ai()} to have been called or model and api info to be supplied.
}
\examples{
\dontrun{
init_sentiment.ai(model = "e5-small")
emb <- embed_text(c("I love this", "this is terrible"))
dim(emb)   # 2 x 384 for e5-small; the "query: " prefix is added internally
}
}
