^.*\.Rproj$
^\.Rproj\.user$
^.*.csv
save_default_data.R
^data-raw$
^README\.Rmd$
^LICENSE\.md$
^inst/tfhub_modules/*
^\.github$
# xgb scorers are ~20MB each (over CRAN's 5MB) -> download on first use, never ship.
# The small mlp/logistic JSON heads DO ship.
^inst/scoring/.*\.xgb$
# the 16MB 0.1.0 default-pole embeddings are legacy USE (512-d); v2 embeds the default
# poles live with e5 and caches them, so this never ships.
^inst/default_embeddings$
^bakeoff$
^planning$
^pubs$
